name: Format Code with Prettier

on:
  pull_request:
    branches:
      - '**'
    types:
      - opened
      - synchronize
      - reopened

jobs:
  prettier:
    if: github.event_name == 'pull_request'

    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0 # Ensure all branches are fetched

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22' # Node.js version 22
          cache: 'npm' # Enable caching for npm

      - name: Install dependencies
        run: npm install

      - name: Run Prettier to check code
        run: npm run format

      - name: Check if files were modified
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "Changes detected, committing and pushing."
            git config user.name "${{ github.actor }}"
            git config user.email "${{ github.actor }}@users.noreply.github.com"
            git checkout ${{ github.head_ref }} # Switch to the branch
            git add .
            git commit -m "Prettified Code!"
            git push origin ${{ github.head_ref }}
          else
            echo "No changes to push."
          fi
